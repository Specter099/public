# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

Miscellaneous public code snippets and utilities. Contains Python scripts for AWS infrastructure tasks such as creating and managing S3 gateway VPC endpoints across all VPCs in an account.

## Setup

```
python3 -m venv .venv && source .venv/bin/activate
pip install boto3
```
